project:
  name: gpt-array-sorter-40
  syncFilters:
    - exclude:
      - .git/*
      - .idea/*
      - '*/__pycache__/*'

containers:
  - projectDir: /workspace/project
    image: python:3.10 # pytorch/pytorch:2.1.0-cuda11.8-cudnn8-runtime
    env:
      PYTHONPATH: /workspace/project

instances:
  - name: aws-1
    provider: aws
    parameters:
      amiId: ami-09477e8d34e7ae7a2
      region: eu-west-2
      instanceType: t2.micro
      volumes:
        - name: workspace
          parameters:
            size: 50


scripts:
  jupyter: |
    jupyter notebook --allow-root --ip 0.0.0.0 --notebook-dir=/workspace/project

 
 