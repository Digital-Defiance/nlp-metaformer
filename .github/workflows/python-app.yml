
name: Test Workflow to Setup Self Hosted Runners

on:
  workflow_dispatch:
  
  

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Test
      run: |
        echo "success"

